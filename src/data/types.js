/**
 * Shared typedefs (JSDoc only).
 * @typedef {'boot'|'menu'|'map'|'encounter'|'combat'|'reward'|'shop'|'rest'|'event'|'game_over'|'settings'} GameStateId
 *
 * @typedef {Object} SaveData
 * @property {string} version
 * @property {string[]} unlockedCharacters
 * @property {Record<string, any>} characterProgress
 * @property {any} globalUnlocks
 * @property {any} settings
 * @property {any|null} currentRun
 */
export {};
